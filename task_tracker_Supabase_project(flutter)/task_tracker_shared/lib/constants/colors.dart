import 'package:flutter/material.dart';

class AppColors {
  // Core app colors
  static const primary = Color(0xFF4A90E2); // Blue for buttons, accents
  static const secondary = Color(0xFFFFA726); // Orange for highlights
  static const background = Color(0xFFF5F7FA); // Light gray background
  static const card = Color(0xFFFFFFFF); // White for cards
  static const textPrimary = Color(0xFF2D3436); // Dark text
  static const textSecondary = Color(0xFF636E72); // Gray text
  static const success = Color(0xFF2ECC71); // Green for success
  static const error = Color(0xFFE74C3C); // Red for errors
  static const darkBackground = Color(0xFF1C2526); // Dark mode background
  static const darkCard = Color(0xFF2D3436); // Dark mode card
  static const darkText = Color(0xFF66BB6A); // Dark mode text

  // ContactPage-inspired colors
  static const gradientDarkStart = Color(0xFF0277BD); // Blue for dark mode gradient
  static const gradientDarkEnd = Color(0xFF2E7D32); // Green for dark mode gradient
  static const gradientLightStart = Color(0xFF42A5F5); // Blue for light mode gradient
  static const gradientLightEnd = Color(0xFF66BB6A); // Green for light mode gradient
  static const accentDark = Color(0xFF00E5FF); // Cyan accent for dark mode
  static const accentLight = Color(0xFF1976D2); // Blue accent for light mode
  static const shadowDark = Color(0x6600E5FF); // Cyan shadow for dark mode
  static const shadowLight = Color(0x4D000000); // Black shadow for light mode
}